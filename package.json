{
  "name": "pixel-text",
  "version": "1.0.0",
  "private": true,
  "description": "Local development scripts for the Pixel Text static app",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch . --ext js,html,css,png,jpg,jpeg,gif,svg,json,md server.js"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
